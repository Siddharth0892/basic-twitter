<div class="container">
  <h1>{{ book.title }}</h1>
</div>
<div class="row">
  <div class="col-md-12">
    <a [routerLink]="['/tweet-edit', tweet._id]" class="btn btn-success">EDIT</a>
    <button class="btn btn-danger" type="button" (click)="deletetweet(tweet._id)">DELETE</button>
  </div>
</div>
