<h1>Tweets</h1>
<input type="text" #ps>

  <button type="button" class="btn btn-primary" (click)="addTweet(ps)">Tweet</button>

<ul>
  <li *ngFor="let t of tweets">
 {{t.title}}
    <button (click)="removeTweet(t)">Remove</button>
  </li>
</ul>
